name: Pack and Publish NuGet (GitHub Packages & Releases)

on:
  workflow_call:  # This makes it reusable
    inputs:
      STABLE_BRANCH_PATTERN:
        description: "Branch pattern for stable releases"
        required: true
        type: string
      GITVERSION_SPEC:
        description: "GitVersion version to use"
        required: false
        type: string
        default: "5.x"
      DOTNET_VERSION:
        description: "Dotnet SDK version to use"
        required: false
        type: string
        default: "8.0.x"
    secrets:
      NUGET_PAT:
        required: true

jobs:
  pack-and-publish:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required for GitVersion

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v0
        with:
          versionSpec: ${{ inputs.GITVERSION_SPEC }}

      - name: Determine Version with Inline Config
        id: gitversion
        uses: gittools/actions/gitversion/execute@v0
        with:
          useConfigFile: false
          configure: |
            mode: ContinuousDeployment
            branches:
              stable:
                regex: ^${{ inputs.STABLE_BRANCH_PATTERN }}$
                tag: ''
                is-release-branch: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ inputs.DOTNET_VERSION }}
          source-url: https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.NUGET_PAT }}

      - name: Restore Dependencies
        run: dotnet restore

      - name: Find and Build Projects
        run: |
          $projects = Get-ChildItem -Path $GITHUB_WORKSPACE -Recurse -Filter *.csproj
          if ($projects.Count -eq 0) {
              Write-Error "No .csproj files found! Check your repository structure."
              exit 1
          }
          foreach ($proj in $projects) {
              Write-Host "Building $proj.FullName"
              dotnet build "$($proj.FullName)" --configuration Release
          }

      - name: Pack NuGet Packages
        run: |
          $projects = Get-ChildItem -Path $GITHUB_WORKSPACE -Recurse -Filter *.csproj
          foreach ($proj in $projects) {
              Write-Host "Packing $proj.FullName with version ${{ steps.gitversion.outputs.FullSemVer }}"
              dotnet pack "$($proj.FullName)" --configuration Release --output nupkgs /p:Version=${{ steps.gitversion.outputs.FullSemVer }}
          }


      - name: Publish NuGet Packages to GitHub Packages
        run: |
          $packages = Get-ChildItem -Path nupkgs -Filter *.nupkg
          foreach ($pkg in $packages) {
              dotnet nuget push $pkg.FullName `
                --source "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" `
                --api-key ${{ secrets.NUGET_PAT }} `
                --skip-duplicate
          }

      - name: Create GitHub Release (Stable Only)
        if: startsWith(github.ref, 'refs/heads/v') || startsWith(github.ref, 'refs/heads/umbraco-')
        env:
          GH_TOKEN: ${{ secrets.NUGET_PAT }}
        run: |
          $TAG_NAME="${{ steps.gitversion.outputs.FullSemVer }}"
          gh release create "$TAG_NAME" nupkgs/*.nupkg --title "Release $TAG_NAME" --notes "Automatically published from GitHub Actions."
