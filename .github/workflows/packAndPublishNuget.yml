name: Pack and Publish NuGet (GitHub Packages & Releases)

on:
  workflow_call:  # This makes it reusable
    inputs:
      PACKAGE_VERSION:
        description: "Version number extracted from Git tag"
        required: true
        type: string
      DOTNET_VERSION:
        description: "Dotnet SDK version to use"
        required: false
        type: string
        default: "8.0.x"
    secrets:
      NUGET_PAT:
        required: true

jobs:
  pack-and-publish:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required for GitVersion

      - name: Strip "v" Prefix from Version
        id: format_version
        shell: pwsh
        run: |
          $VERSION = "${{ inputs.PACKAGE_VERSION }}"
          $VERSION = $VERSION -replace "^v", ""  # remove the "v" from version tag or manual input
          echo "VERSION=$VERSION" >> $env:GITHUB_ENV


      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ inputs.DOTNET_VERSION }}
          source-url: https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.NUGET_PAT }}

      - name: Restore Dependencies
        run: dotnet restore

      - name: Build and Pack NuGet Packages
        run: |
          $projects = Get-ChildItem -Path $GITHUB_WORKSPACE -Recurse -Filter *.csproj
          foreach ($proj in $projects) {
              Write-Host "Packing $proj with version $VERSION"
              dotnet pack "$($proj.FullName)" --configuration Release --output nupkgs /p:Version=$VERSION
          }


      - name: Publish NuGet Packages to GitHub Packages (Replace Existing)
        run: |
          $packages = Get-ChildItem -Path nupkgs -Filter *.nupkg
          foreach ($pkg in $packages) {
              dotnet nuget push $pkg.FullName `
                --source "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" `
                --api-key ${{ secrets.NUGET_PAT }} `
                --skip-duplicate
          }

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "$VERSION" nupkgs/*.nupkg --title "Release $VERSION" --notes "Automatically published from GitHub Actions."
